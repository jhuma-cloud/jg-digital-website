# Projects

Case studies, portfolio pieces, and longer internal initiatives — not day-to-day client delivery (use `clients/`).

For shorter agency-owned campaign work, use `campaigns/` instead.

## Structure

- `case-studies/` — sanitized success stories for sales and marketing
- Add dated project folders: `YYYY-MM-project-slug/` as needed
