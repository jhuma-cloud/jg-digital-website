# Documentation

Agency knowledge base: brand, onboarding, and SOPs.

| Folder | Purpose |
|--------|---------|
| `brand/` | JG Digital identity, tone, visual rules |
| `onboarding/` | How we onboard clients and contractors |
| `sops/` | Repeatable processes (reporting, launches, QA) |

Keep docs short and actionable. Link to templates in `/templates` instead of duplicating forms.
