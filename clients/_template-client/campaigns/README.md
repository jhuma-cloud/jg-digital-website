# Campaigns

One subfolder or file set per initiative: `YYYY-MM-campaign-slug/`.

Include:

- Objectives and KPIs (link `analytics/kpi-frameworks/`)
- Channel plan
- Creative references (`../assets/` or `creative/`)
- Launch checklist (`tools/checklists/campaign-launch.md`)
