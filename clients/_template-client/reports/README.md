# Reports

Performance reporting for this client. Copy `templates/report.md` per period.

Naming: `YYYY-MM-report.md` or `YYYY-MM-DD-weekly-snapshot.md`

Store exports (PDF/slides) alongside markdown source when needed; large binaries may stay out of git per `.gitignore`.
